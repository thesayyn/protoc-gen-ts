{
    "name": "example",
    "scripts": {
        "test": "protoc -I=src --ts_opt=target=web --ts_out=src weather.proto && tsc && node ./dist/index"
    },
    "dependencies": {
        "@grpc/grpc-js": "^1.2.12",
        "google-protobuf": "^3.15.8"
    },
    "devDependencies": {
        "protoc-gen-ts": "file:../../",
        "grpc-web": "^1.3.0",
        "typescript": "^4.2.4"
    }
}