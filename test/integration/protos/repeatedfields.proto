syntax = "proto3";

message RepeatedMessages {
    repeated string test = 1;
    repeated int64 a = 2;
    repeated bytes b = 3;

    repeated Sub repeatedSub = 4;

    Sub notrepeated = 5;

    message Sub {
        repeated string test = 1;
    }
}

